<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bjs.zangbu.member.mapper.MemberMapper">
    <!--새로운 비밀번호로 변경-->
    <update id="updatePassword">
        update member
        set password = #{newPassword}
        where member_id = #{memberId}
    </update>

    <!--새로운 닉네임으로 변경-->
    <update id="updateNickname">
        update member
        set nicename = #{newNickname}
        where member_id = #{memberId}
    </update>

    <!--bookmark 삭제-->
    <delete id="deleteBookMark">
        delete from bookmark
        where member_id = #{memberId}
          and building_id = #{buildingId}
    </delete>

    <!--회원 정보 삭제-->
    <delete id="deleteMemberId">
        delete from member
        where member_id = #{memberId}
    </delete>

    <!--회원 정보 가져오기-->
    <select id="get" resultType="bjs.zangbu.security.account.vo.Member">
        select email, nickname
        from member
        where member_id = #{memberId}
    </select>

    <!--기존 비밀번호 가져오기-->
    <select id="findPasswordByMemberId" resultType="java.lang.String">
        select password
        from member
        where member_id = #{memberId}
    </select>

    <!--닉네임 중복 확인하기-->
    <select id="countByNickname" resultType="java.lang.Integer">
        select count(*)
        from member
        where nickname = #{nickname}
    </select>

    <!--id에 따른 북마크 매물 가져오기-->
    <select id="getBookmarksByMemberId" resultType="bjs.zangbu.member.dto.join.BookmarkBuilding">

    </select>

    <!--현재 닉네임 가져오기-->
    <select id="getNicknameByMemberId" resultType="java.lang.String">
        select nickname
        from member
        where member_id = #{memberId}
    </select>


</mapper>
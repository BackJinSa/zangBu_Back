# 네트워크
bind 0.0.0.0
port 6379
protected-mode yes

# 영속화 (RDB)
save 900 1
save 300 10
save 60 10000

# AOF (원하면 yes로 변경)
appendonly no

# 메모리 정책 (필요 시 조정)
maxmemory 256mb
maxmemory-policy allkeys-lru

# 보안 (원하면 활성화)
# requirepass <초강력_비밀번호>
